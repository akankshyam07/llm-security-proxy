fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
pyyaml>=6.0
python-dotenv>=1.0
# PII detection — falls back to regex if spacy model is not downloaded
presidio-analyzer>=2.2.0
spacy>=3.7.0
# Secret scanning (30+ plugin patterns)
detect-secrets>=1.4.0
